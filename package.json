{
  "name": "RISC-V LSP",
  "description": "Extension for integrating RISC-V LSP with Vscode for RISC-V Programs",
  "author": "carn181",
  "license": "MIT",
  "version": "1.0.0",
  "publisher": "carn181",
  "categories": [],
  "engines": {
    "vscode": "^1.75.0"
  },
  "activationEvents": [
    "onLanguage"
  ],
  "main": "./client/out/extension",
  "contributes": {
    "configuration": {}
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "postinstall": "cd client && npm install && cd .."
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "typescript": "^5.2.2"
  }
}
